name: vijayworkflowcontext
on: 
 push:
  branches:
    - "main"
jobs:
  building:
     runs-on: ubuntu-latest
     steps:
      - name: clone the repo
        uses: actions/checkout@v4
      
   

  
